using System.Collections.Generic;

namespace Markdown;
public class MdParser
{
    private List<string> Parse(string text)
    {
        return default;
    }

    public List<Token> SplitIntoTokens(string text)
    {
        var parsedText = Parse(text);

        return default;
    }
}
